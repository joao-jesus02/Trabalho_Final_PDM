<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/bestiario_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bestiario_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.BDRoomComCamera"
        tools:targetApi="31">
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ActivityInfo"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityRacas"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityFichaRaca"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityPesquisaPersonagens"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityRecuperarSenha"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityLogin"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityCadastroUsuario"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.bdroomcomcamera.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
